version: "3.8"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules 
      - ./logs:/app/logs
    environment:
      - NODE_ENV=development
