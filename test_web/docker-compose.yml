version: "3.8"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # This correctly maps the 'logs' folder from your project directory
      # to the '/app/logs' folder inside the container.
      - ./logs:/app/logs